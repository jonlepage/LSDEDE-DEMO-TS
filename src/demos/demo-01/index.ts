/**
 * Demo 01 — placeholder.
 * Each demo exports a single `run` function that composes engine + renderer.
 */

import type { DemoManifestEntry } from "../../shared/types";

export const DEMO_MANIFEST: DemoManifestEntry = {
  id: "demo-01",
  title: "Demo 01",
  description: "Placeholder — blueprint not yet exported.",
  blueprintPath: "/blueprints/demo-01/blueprint.json",
};

export async function runDemo(): Promise<void> {}
